name: ENV Variables
on: push
env:
  WF_SEC: ${{ secrets.WF_SEC }}
jobs:
  log_env:
    runs-on: ubuntu-latest
    steps:
      - name: 'log env variables'
        run: |
          echo "WF_ENV: ${WF_ENV}"
         

